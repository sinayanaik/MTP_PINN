# EDR package marker — intentionally empty.
# The hyphenated directory name is not a valid Python identifier; callers that
# need to import from this package must add its parent to sys.path first.
# See run_edr.py for the recommended pattern.
