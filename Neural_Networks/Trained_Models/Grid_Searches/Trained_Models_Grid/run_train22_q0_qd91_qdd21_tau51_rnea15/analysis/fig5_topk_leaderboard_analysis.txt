This figure presents three comprehensive tables that act as a "leaderboard," ranking the top 10 hyperparameter search runs (sorted by Test RMSE) for each of the three evaluated architectures: Black-Box (out of 12 total runs), Physics-Reg (out of 72 total runs), and Residual-Corr (out of 60 total runs).

Black-Box Table: The best-performing run for this architecture achieves a Test RMSE of 0.1003 N·m and a Test R² of 0.8932, requiring 300 epochs. While performance across its top 10 runs is relatively stable (Test RMSE ranging from 0.1003 to 0.1031), notably, none of these optimized runs manage to surpass the baseline performance of the top physics-informed models.

Physics-Reg Table: The best run here achieves a Test RMSE of 0.0923 N·m and a Test R² of 0.9080 at epoch 1075. The top runs frequently utilize substantial physics loss weights (Phys-W), often 0.5 or 0.2, indicating the importance of strong physical regularization. The Test RMSE across the top 10 ranges narrowly from 0.0923 to 0.0932 N·m, demonstrating a consistent and strong performance cluster resulting from the hyperparameter sweep.

Residual-Corr Table: The Residual-Corr architecture dominates the leaderboard. Its absolute best run achieves an impressive Test RMSE of 0.0903 N·m and a Test R² of 0.9106 at epoch 339. The top runs utilize various alpha-Reg parameters (e.g., 0.1, 0.05, 0.01). The performance is remarkably tightly clustered; in fact, the 10th best run in the Residual-Corr table achieves a Test RMSE of 0.0912 N·m, which still outperforms the very best run from the Physics-Reg architecture.

Conclusion: The tabular leaderboard explicitly quantifies the dominance of the Residual-Corr architecture. Its top 10 hyperparameter configurations consistently and decisively outperform the best runs from both the Physics-Reg and Black-Box architectures. Furthermore, the results indicate that the hyperparameter search yielded highly stable and robust performance clusters for the physics-informed models, reinforcing their reliability and superiority over purely data-driven approaches across a wide spectrum of configurations.